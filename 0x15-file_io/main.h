#ifndef _MAIN_H
#define _MAIN_H

#include <unistd.h>
#include <stdlib.h>
#include <string.h>
#include <stdio.h>

int _putchar(const char c);
ssize_t read_textfile(const char *filename, size_t letters);

#endif
