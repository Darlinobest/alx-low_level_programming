#include "main.h"
/**
 * _islower - Entry point
 *
 * This program check for lowercase
 *
 * Return: Always 0
 */
int _islower(int c) /* int c, input to the function */
{
	return (c >= 'a' && c <= 'z');
}
