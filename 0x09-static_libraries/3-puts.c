#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _puts - print a string to standard output
 * @str: pointer to character
 * Return: Always 0
 */

void _puts(char *str)
{
	puts(str);
}
