#include "lists.h"

list_t *add_node_end(list_t **head, const char *str)
{
	list_t *temp;

	if (head == NULL && str == NULL)
		return (NULL);
	temp = malloc(sizeof(list_t);
	if (temp == NULL)
		return (NULL);
	
}
